apiVersion: v1
kind: Service
metadata:
  name: ollama
  namespace: ai-stack
  labels:
    app: ollama
    app.kubernetes.io/part-of: ai-stack
spec:
  type: ClusterIP
  selector:
    app: ollama
  ports:
    - name: http
      port: 11434
      targetPort: http
      protocol: TCP
