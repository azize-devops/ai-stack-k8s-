apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-anythingllm
  namespace: ai-stack
spec:
  podSelector:
    matchLabels:
      app: anythingllm
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # Allow from ingress controller
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
      ports:
        - protocol: TCP
          port: 3001
  egress:
    # Allow access to LocalAI
    - to:
        - podSelector:
            matchLabels:
              app: localai
      ports:
        - protocol: TCP
          port: 8080
    # Allow access to Qdrant
    - to:
        - podSelector:
            matchLabels:
              app: qdrant
      ports:
        - protocol: TCP
          port: 6333
        - protocol: TCP
          port: 6334
    # Allow access to Ollama
    - to:
        - podSelector:
            matchLabels:
              app: ollama
      ports:
        - protocol: TCP
          port: 11434
    # Allow DNS resolution
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
